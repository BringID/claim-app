import styled from "styled-components"

export const ExtraSubtitleComponent = styled.h3`
  color: ${props => props.theme.primaryTextColor};
  margin: 0;
  font-size: 16px;
  line-height: 24px;
  font-weight: 700;
`
