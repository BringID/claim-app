import styled from "styled-components"

export const SubtitleComponent = styled.h3`
  color: ${props => props.theme.primaryTextColor};
  margin: 0;
  font-size: 24px;
  line-height: 32px;
  font-weight: 700;
  
`
