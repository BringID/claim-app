import styled from "styled-components"
import {
  CurrentScore,
} from '../../components'
import {
  Subtitle,
  Input,
  SmallText
} from '../../../components/common'


export const CurrentScoreStyled = styled(CurrentScore)`
  margin-bottom: 24px;
`

export const InputStyled = styled(Input)`
  margin: 0 0 8px;
`

export const SubtitleStyled = styled(Subtitle)`
  font-size: 16px;
  margin: 0 0 8px;
`

export const SmallTextStyled = styled(SmallText)``